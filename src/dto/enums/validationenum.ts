export enum ministryType {
  music,
  preaching,
  others
}

export enum coreType {
  music,
  sermon,
  music_sermon
}

export enum office {
  apostle,
  prophet,
  evangelist,
  pastor,
  teacher,
  minstrel
}

export enum mediaType {
  music,
  sermon,
  prayer,
  post
}
